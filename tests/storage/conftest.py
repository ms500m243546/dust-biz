"""Storage-suite fixtures.

The shared `engine` + `session` fixtures live in `tests/conftest.py`
so the domain suite can reuse them. This file is intentionally empty -
kept so future storage-only fixtures (e.g. seeded mine state) have an
obvious home.
"""
